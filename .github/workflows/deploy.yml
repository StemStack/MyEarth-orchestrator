name: Deploy to MyEarth Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          version: "stable"

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 100.69.50.87
          username: jc
          key: ${{ secrets.DEPLOY_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 600s
          script: |
            set -e
            echo "🚀 Starting deployment..."
            cd /home/jc

            echo "📥 Pulling latest changes..."
            if [ -d MyEarth ]; then
              cd MyEarth
              git fetch origin
              git reset --hard origin/main
            else
              git clone https://github.com/StemStack/MyEarth.git
              cd MyEarth
            fi

            echo "🏷  Updating version.json from footer"
            export ENVIRONMENT=production
            python3 update_version.py || true

            echo "📦 Rebuilding Python virtualenv..."
            rm -rf venv
            python3 -m venv venv
            source venv/bin/activate
            python -m pip install -U pip setuptools wheel
            python -m pip install -r requirements.txt

            echo "🧹 Normalize root index.html (prefer real HTML, not pointer)"
            ROOT=/home/jc/MyEarth
            CAND=$(find "$ROOT" -maxdepth 3 -type f -name index.html -printf '%s %p\n' | sort -nr | head -1 | cut -d' ' -f2-)
            if [ -n "$CAND" ]; then cp "$CAND" "$ROOT/index.html"; fi
            perl -0777 -i -pe 'BEGIN{$a=q{<script src="/static/js/buildings3d.js"></script>\n<script>try{window.Buildings3DController&&window.Buildings3DController.init(window.viewer);}catch(e){console.warn("B3D init",e)}</script>\n};} unless(/buildings3d\.js/i){ s#</body>#$a</body>#i or $_ .= "\n$a\n"; }' "$ROOT/index.html" || true

            echo "🔄 Restarting MyEarth service..."
            sudo systemctl restart myearth

            echo "⏳ Waiting for service to start..."
            sleep 10

            echo "📋 Checking version:"
            curl -sL https://myearth.app/version.json || true
            echo "✅ Deployment completed!"

      - name: Health check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 100.69.50.87
          username: jc
          key: ${{ secrets.DEPLOY_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 120s
          request_pty: true
          script: |
            echo "🏥 Starting health check..."
            sleep 15
            echo "📊 Service status:"
            sudo systemctl --no-pager -l status myearth | cat || true
            echo "🌐 Testing app response..."
            if curl -f -sL https://myearth.app/api/ping > /dev/null; then
              echo "✅ App is responding on domain"
            else
              echo "❌ App not responding on domain"
            fi
            echo "📋 Checking version:"
            curl -sL https://myearth.app/version.json || echo "Version endpoint not accessible"
            echo "🏁 Health check completed!"
